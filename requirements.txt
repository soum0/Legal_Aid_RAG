# Core UI
streamlit==1.28.1

# LangChain (working compatible versions)
langchain==0.1.20
langchain-community==0.0.38
langchain-core==0.1.52

# Vector DB
chromadb==1.5.0

# Embeddings
sentence-transformers==5.2.2

# LLM provider (Groq via OpenAI-compatible client)
openai==2.21.0

# Environment management
python-dotenv==1.0.1

# Core dependencies (explicitly pinned for Streamlit Cloud stability)
pydantic==2.6.4
numpy>=1.24.0,<2.0.0
requests>=2.31.0
